export default defineAppConfig({
  nuxtIcon: {
    size: '14px', // default <Icon> size applied
    class: 'svg-icon', // default <Icon> class applied
    aliases: {
      'nuxt': 'logos:nuxt-icon',
    }
  }
})
